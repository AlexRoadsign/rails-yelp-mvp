<h1> The 5 best Restaurants in the City</h1>

<ul>
  <% @restaurants.each do |restaurant| %>
  <li><%= restaurant.name %> (<%= link_to 'details', restaurant_path(restaurant) %>)</li>
  <% end %>
</ul>

<%= link_to 'Add a restaurant', new_restaurant_path %>
